{
  "name": "bikeshop",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --scope=@bikeshop/presentation-web",
    "build": "lerna run build --stream --since master",
    "test": "lerna run test --stream --since master"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
